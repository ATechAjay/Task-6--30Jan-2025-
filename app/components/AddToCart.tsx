"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("Added to the cart!")}>
        Add to cart
      </button>
    </div>
  );
};

export default AddToCart;
